jobs:
  build:
    docker:
      - image: node:16.15.0
    environment:
      repo_token: HcdbrGa6dvGSRqnUPUqmfB7UjBk9T2Dcg
  
    steps:
      - checkout:
      - run:
          name: Install dependencies
          command: npm install
      - run:
          name: Run tests
          command: npm test --coverage
      - run:
          name: Upload coverage to Coveralls
          command: npx coveralls < coverage/lcov.info


